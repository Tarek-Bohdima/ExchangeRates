<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Minimal platform theme. Compose's MaterialTheme handles the actual
        UI styling — this theme only needs to give the activity a window
        background and no action bar so Compose can take over from edge to edge.
    -->
    <style name="Theme.ExchangeRates" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
